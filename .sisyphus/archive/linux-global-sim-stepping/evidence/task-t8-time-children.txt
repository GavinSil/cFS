T8 TIME service step isolation verification (PASSED)
Date: 2026-03-16

Source of truth:
- .sisyphus/plans/linux-global-sim-stepping.md (T8 QA scenario: TIME服务不脱离step独立推进)
- .sisyphus/notepads/linux-global-sim-stepping/learnings.md (T8 repeated section)
- psp/fsw/modules/sim_stepping/cfe_psp_sim_stepping_core.c

TIME task-cycle implementation (T8 fix)
1) Before fix:
   - ReportTimeTaskCycle() added trigger with source_mask 0x08
   - This over-represented TIME in wait-set

2) After fix (current):
   - ReportTimeTaskCycle() is non-blocking fact hook
   - No trigger creation
   - TIME represented only via core-service receive/complete (service 0x02)
   
Code location: lines 245-257 in cfe_psp_sim_stepping_core.c
```c
/* Non-blocking fact hook: TIME main-task cycle is reported but does not 
 * create a blocking trigger. The TIME main task is already represented 
 * in the wait-set via paired core-service receive/completion facts.
 */
```

Sim time source
- All TIME queries go through CFE_PSP_GetTime() hook
- Hook returns sim_time_ns from stepping core
- No wall-clock fallback during active sessions

Runtime evidence
- T13 idle observation: TIME output doesn't advance without steps
- T14 scenario observations: TIME-derived timestamps advance with steps

Interpretation vs T8 acceptance:
- PASS: TIME service doesn't advance independently of steps
- PASS: Task-cycle hook is non-blocking (informational only)
- PASS: TIME shares unified sim_time_ns with all other components

Conclusion
T8 TIME isolation verification PASSED. TIME service coordinated with step advancement.
