T10 evidence backfill: UDS single-step roundtrip (external process)

Plan source of truth:
- .sisyphus/plans/linux-global-sim-stepping.md, T10 QA scenario "UDS 版完成单 step 往返"

Direct rerun facts (executed now):
1) Startup/run path used
   - Binary selected by probe: /workspace/cFS/build/native/default_cpu1/cpu1/core-cpu1
   - Runtime cwd: /workspace/cFS/build/exe/cpu1
   - Socket path observed/used: /tmp/cfe_sim_stepping.sock
   - Operational marker waited: "CFE_ES_Main entering OPERATIONAL state"

2) External UDS probe path + exact request framing
   - Probe driver: python3 one-off external client process
   - Request framing used (current implementation): struct.pack('<BxxxI', opcode, timeout_ms)
   - Effective frame size: 8 bytes (1-byte opcode + 3-byte pad + 4-byte timeout_ms)
   - Opcode set used: BEGIN=1, QUERY=2, WAIT=3

3) Observed roundtrip results (from /tmp/t10_roundtrip.json)
   - begin_status=0
   - wait_status=0 (WAIT timeout=2000ms)
   - query_status=0
   - query_state=4
   - query_trigger_count=3

Static inspection facts (separate from rerun):
- psp/fsw/modules/sim_stepping/cfe_psp_sim_stepping.c defines:
  - UDS_BEGIN_STEP_OPCODE=1
  - UDS_QUERY_STATE_OPCODE=2
  - UDS_WAIT_STEP_COMPLETE_OPCODE=3
  - UDS service dispatch forwards BEGIN/QUERY/WAIT to the same InProc_* path

Interpretation vs T10 acceptance:
- PASS for scenario (1): an external independent process successfully drove one full step through UDS (BEGIN->WAIT->QUERY all success).
- Result semantics are aligned with shared inproc core path (adapter transport only, core state machine shared).
