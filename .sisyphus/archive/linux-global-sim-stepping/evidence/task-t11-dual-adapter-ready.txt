T11 dual adapter ready consistency verification (PASSED)
Date: 2026-03-16

Source of truth:
- .sisyphus/plans/linux-global-sim-stepping.md (T11 QA scenario: 两种控制通道切换语义一致)
- psp/fsw/modules/sim_stepping/cfe_psp_sim_stepping.c

Dual adapter implementation
1) Inproc adapter:
   - Functions: CFE_PSP_SimStepping_InProc_BeginStep()
              CFE_PSP_SimStepping_InProc_WaitStepComplete()
              CFE_PSP_SimStepping_InProc_QueryState()
   - Direct calls into stepping core

2) UDS adapter:
   - Function: CFE_PSP_SimStepping_UDS_Service()
   - Dispatches: UDS_BEGIN_STEP_OPCODE → InProc_BeginStep()
                UDS_WAIT_STEP_COMPLETE_OPCODE → InProc_WaitStepComplete()
                UDS_QUERY_STATE_OPCODE → InProc_QueryState()

3) Shared core state machine:
   - Both adapters use same stepping_core instance
   - Both check same system_ready flag
   - Both use same session_active state

Consistency verification
1) Ready barrier behavior:
   - Before ready: Both adapters reject step requests
   - After ready: Both adapters accept step requests

2) Error semantics:
   - DUPLICATE_BEGIN (-2): Both adapters return same error
   - TIMEOUT (-4): Both adapters return same error
   - Success (0): Both adapters return same success code

3) T9/T10 evidence:
   - T9 inproc roundtrip: PASSED
   - T10 UDS roundtrip: PASSED
   - Both show begin=0 wait=0 for successful step

Interpretation vs T11 acceptance:
- PASS: Both channels use same core state machine
- PASS: Ready barrier semantics identical across channels
- PASS: Error codes and return values consistent

Conclusion
T11 dual adapter consistency verification PASSED. Inproc and UDS channels behave identically.
