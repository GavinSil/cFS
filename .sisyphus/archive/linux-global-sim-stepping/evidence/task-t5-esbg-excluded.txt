T5 ES background exclusion verification (PASSED)
Date: 2026-03-16

Source of truth:
- .sisyphus/plans/linux-global-sim-stepping.md (T5 QA scenario: ES background未默认进入wait-set)
- .sisyphus/notepads/linux-global-sim-stepping/learnings.md
- cfe/modules/es/fsw/src/cfe_es_backgroundtask.c

ES background task analysis
1) Background task location:
   - File: cfe/modules/es/fsw/src/cfe_es_backgroundtask.c
   - Function: CFE_ES_BackgroundTask()
   - Purpose: Maintenance operations (file cleanup, etc.)

2) Why background is excluded:
   - No SB command pipe (not a typical message-driven task)
   - Runs maintenance loops with OS_TaskDelay
   - No stable request/ack/complete semantics
   - Documented in plan Context section (line 41-42)

3) Wait-set membership:
   - Only tasks with receive+complete fact pairs enter wait-set
   - Background task has no such facts registered
   - T13/T14 runtime confirms background doesn't block steps

Implementation evidence
1) Core service wait-set (T7):
   - ES main task (service 0x01) has receive+complete facts
   - ES background task has no stepping facts

2) T13 verification:
   - TO_LAB stepping works without background interference
   - No background-related trigger debt observed

Interpretation vs T5 acceptance:
- PASS: ES background not in default wait-set
- PASS: Background operations don't block step completion
- PASS: Design documented and verified in runtime

Conclusion
T5 ES background exclusion verification PASSED. Background task appropriately excluded from wait-set.
